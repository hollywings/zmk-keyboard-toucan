# SPDX-License-Identifier: MIT
#
# Display hardware for the Toucan dongle is configured entirely through
# devicetree (ST7789V controller, SPI, backlight PWM, ambient sensor). The
# dongle_screen_host shield owns the status screen and theme renderer, so this
# shield contributes no compile sources.
